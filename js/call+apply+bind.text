存在的原因
apply() 和 call() 的存在提供了极大的灵活性，允许开发者在不同的对象间重用函数。这些方法非常有用，特别是在你想控制函数调用上下文的场景中。它们的使用场景包括：

对象方法借用：一个对象可以借用另一个对象的方法，而不需要复制代码。
函数绑定：可以用 call() 或 apply() 在特定的上下文中永久绑定函数。
变参函数调用：可以用 apply() 调用一个函数，并传递一个参数数组，特别适合不确定参数数量的情况。