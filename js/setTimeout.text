终于明白为什么要清除定时器了

是不是这样的, 如果setinterval不清除的话, 浏览器会每隔一段时间都会把里面的回调函数放到队列里,然后主线程会去执行
ChatGPT

那settimeout这为什么也要被清除

1. 避免不必要的执行
条件改变：如果在 setTimeout 设定执行的延时期间内，应用的状态或者相关条件发生了变化（例如用户已经离开了当前页面，或者操作已经不再需要），那么执行这个 setTimeout 的回调可能变得不再适当或甚至可能导致错误。


